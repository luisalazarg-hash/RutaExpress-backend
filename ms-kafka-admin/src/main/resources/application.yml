server.port: ${SERVER_PORT:8087}
spring:
  application.name: ms-rutaexpress-kafka-admin
  kafka.bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
management.endpoints.web.exposure.include: health,info
